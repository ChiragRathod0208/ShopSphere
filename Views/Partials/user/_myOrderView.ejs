<!-- <div class="p-3 w-full flex flex-col h-full">
    <%// orders.forEach(order => { %>
        <div class="w-full bg-gray-200 h-[80%] my-10 px-10 py-10">
            <div>
                <h2>Order Id: <%=// order._id%></h2>
            </div>
            <%// (order.Items).forEach(item =>  { %>
                <div class="flex items-start gap-10">
                    <div class="w-[30%] rounded-md overflow-hidden">
                        <div class="w-full h-60 bg-yellow-500"></div>
                        <div class="w-full flex justify-between px-5 py-4">
                            <h3 class="text-2xl"><%=// item.name %></h3>
                        </div>
                        <div class="flex items-center px-5 py-3">
                            <h4 class="text-lg">Net Total</h4>
                            <h2 class="ml-6 text-lg">₹ 1200</h2>
                        </div>
                    </div>
                    <div class="w-[70%]">
                        <h3 class="text-xl">Price Breakdown</h3>
                        
                        <div class="px-10 mt-5">
                            <div class="flex mt-2">
                            <h4 class="w-1/3">Total MRP</h4>
                            <h4>₹ 123<%=// Item.product.price %></h4>
                        </div>
                        
                        <div class="flex mt-2">
                            <h4 class="w-1/3">Discount on MRP</h4>
                            <h4>₹ 0</h4>
                        </div>
                        <div class="flex mt-2">
                            <h4 class="w-1/3">Platform Fee</h4>
                            <h4>₹ 20</h4>
                        </div>
                        <div class="flex mt-2">
                            <h4 class="w-1/3">Shipping Fee</h4>
                            <h4>FREE</h4>
                        </div>
                    </div>
                    <div class="w-full h-[1px] bg-black mt-10"></div>
                    <div class="flex mt-5">
                        <h3 class="w-1/3 text-xl">Total Amount</h3>
                    <h3 class="font-semibold text-xl text-green-600">₹ 30<%=// (Item.product.price) * Item.quantity %></h3>
                </div>
            </div>
        <% //}) %>
        </div>
    <% //}) %>
</div> -->



<div class="space-y-6">

  <h2 class="text-2xl font-semibold text-slate-700">
    My Orders
  </h2>

  <% if (!orders || orders.length === 0) { %>
    <p class="text-slate-500">You have not placed any orders yet.</p>
  <% } %>

  <% orders.forEach(order => { %>
    <div class="border rounded-xl bg-white shadow-sm p-5">

      <!-- Order header -->
      <div class="flex justify-between items-center mb-4">
        <div>
          <p class="text-sm text-slate-500">
            Order ID:
            <span class="font-medium text-slate-700">
              <%= order._id %>
            </span>
          </p>
          <p class="text-sm text-slate-500">
            Placed on: <%= order.createdAt.toDateString() %>
          </p>
        </div>

        <div class="text-lg font-semibold text-blue-600">
          ₹ <%= order.totalAmount %>
        </div>
      </div>

      <!-- Order items -->
      <div class="space-y-4">
        <% order.Items.forEach(item => { %>
          <div class="flex items-center justify-between border-b pb-3">

            <div class="flex items-center gap-4">
              <!-- Product Image -->
              <img
                src="<%= item.image %>"
                alt="<%= item.name %>"
                class="w-16 h-16 object-cover rounded-lg border"
              />

              <!-- Product Info -->
              <div>
                <p class="font-medium text-slate-700">
                  <%= item.name %>
                </p>
                <p class="text-sm text-slate-500">
                  ₹ <%= item.price %> × <%= item.quantity %>
                </p>
              </div>
            </div>

            <!-- Item Total -->
            <div class="font-medium text-slate-700">
              ₹ <%= item.price * item.quantity %>
            </div>

          </div>
        <% }) %>
      </div>

    </div>
  <% }) %>

</div>
