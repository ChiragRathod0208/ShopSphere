<!-- <div class="bg-white shadow ml-4">
    <div class="pt-4 mt-4 border-t-2 border-blue-200"></div>
    <h2 class="px-2 py-2 text-xl font-bold">Order List</h2>
    <div class="pt-4 mt-4 border-t-2 border-blue-200"></div>
    <div class="relative overflow-x-auto bg-neutral-primary-soft shadow-xs rounded-base border border-default">
        <table class="w-full text-sm text-left rt1:text-right text-body">
            <thead class="bg-neutral-secondary-soft border-b border-default">
                <tr>
                    <th scope="col" class="px-6 py-3 font-medium">Sr No.</th>
                    <th scope="col" class="px-6 py-3 font-medium">Order Id</th>
                    <th scope="col" class="px-6 py-3 font-medium">User Name</th>
                    <th scope="col" class="px-6 py-3 font-medium">Items</th>
                    <th scope="col" class="px-6 py-3 font-medium">Total Amount</th>
                </tr>
            </thead>
            <tbody>
                <%// orders.forEach((order, idx) => { %>
                    <tr class="odd:bg-neutral-primary even:bg-neutral-secondary-soft border-b border-default text-base">
                        <td class="px-6 py-4"><%=// idx + 1 %></td>
                        <td class="px-6 py-4"><%=// order._id %></td>
                        <td class="px-6 py-4"><%=// product.image %></td>
                        <td class="px-6 py-4"><%=// product.price %></td>
                        <td class="px-6 py-4"><%=// product.bgColor %></td>
                        <td class="px-6 py-4"><%=// product.panelColor %></td>
                        <td class="px-6 py-4"><%=// product.textColor %></td>
                        <td class="px-6 py-4"><%=// product.stock %></td>
                        <td class="px-6 py-4">
                            <form id="editForm" method="post">
                                <input type="text" id="id" name="id" value=<%=// product._id %> hidden>
                                <button type="submit" class="bg-yellow-400 rounded-md my-2 px-3 py-3"><i class="fa-solid fa-pen-to-square"></i></button>
                            </form>
                            <button class="bg-yellow-400 rounded-md my-2 px-3 py-3"><i class="fa-solid fa-pen-to-square" onclick="editProduct('<%=// product._id %>')"></i></button>
                            <button class="bg-red-600 rounded-md my-2 px-3 py-3" onclick="deleteProduct('<%=// product._id %>')"><i class="fa-solid fa-trash"></i></button>
                        </td>
                    </tr>
                <%// }) %>
        </table>

    </div>
</div>
 -->



 <div class="space-y-6">

  <h2 class="text-2xl font-semibold text-slate-700">
    All Orders
  </h2>

  <% if (!orders || orders.length === 0) { %>
    <p class="text-slate-500">No orders found.</p>
  <% } %>

  <% orders.forEach(order => { %>
    <div class="border rounded-lg bg-white shadow-sm p-5">

      <!-- Order header -->
      <div class="flex justify-between items-start mb-4">
        <div>
          <p class="text-sm text-slate-500">
            Order ID:
            <span class="font-medium text-slate-700"><%= order._id %></span>
          </p>
          <p class="text-sm text-slate-500">
            User:
            <span class="font-medium text-slate-700">
              <%= order.user?.name %>
            </span>
            (<%= order.user?.email %>)
          </p>
          <p class="text-sm text-slate-500">
            Date: <%= order.createdAt.toDateString() %>
          </p>
        </div>

        <div class="text-lg font-semibold text-blue-600">
          ₹ <%= order.totalAmount %>
        </div>
      </div>

      <!-- Order items -->
      <div class="space-y-2">
        <% order.Items.forEach(item => { %>
          <div class="flex justify-between border-b pb-2 text-sm">
            <div>
              <p class="font-medium text-slate-700">
                <%= item.name %>
              </p>
              <p class="text-slate-500">
                ₹ <%= item.price %> × <%= item.quantity %>
              </p>
            </div>

            <div class="font-medium text-slate-700">
              ₹ <%= item.price * item.quantity %>
            </div>
          </div>
        <% }) %>
      </div>

    </div>
  <% }) %>

</div>
